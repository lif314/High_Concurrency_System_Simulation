spring:
  cloud:
    gateway:
      routes:
        # product
        - id: product_route
          uri: lb://gulimall-product
          predicates:
            - Path=/api/product/**
          filters:
            - RewritePath=/api/(?<segment>.*),/$\{segment}

    sentinel:
      transport:
        port: 8719
        dashboard: 139.198.167.224:31267
